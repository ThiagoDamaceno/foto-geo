Recursos do empacotamento (`electron-builder` · `directories.buildResources`).

Coloque aqui, quando tiver:

- `icon.ico` — ícone do `.exe` (Windows), ideal 256×256
- `icon.png` — opcional (256×256)

Sem ícone, o builder usa o padrão do Electron.
